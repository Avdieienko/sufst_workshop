#include <stdio.h>

// Step 0: import CarSensors.h and drive.h from Inc folder

int main() {
  // Step 1: Create a CarSensors object

  // Step 2: Initialize the CarSensors object

  // Step 3: Print the sensor values

  // Step 4: Call the drive function

  return 0;
}